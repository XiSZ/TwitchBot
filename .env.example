# Required Twitch auth
# Generate an OAuth token via Twitch's official OAuth flow:
#   Docs: https://dev.twitch.tv/docs/authentication/getting-tokens-oauth/
#   Helper: run `npm run get-token` after setting TWITCH_CLIENT_ID and TWITCH_REDIRECT_URI.
# Important: `TWITCH_USERNAME` must match the account that generated the token,
# and `TWITCH_OAUTH_TOKEN` must include the `oauth:` prefix.
TWITCH_USERNAME=your_bot_username
TWITCH_OAUTH_TOKEN=oauth:your_generated_token

# Comma-separated list of channels to join (JSON array format, without #)
TWITCH_CHANNELS=["channel1", "channel2"]

# OAuth app config (for local token helper)
TWITCH_CLIENT_ID=your_twitch_app_client_id
TWITCH_REDIRECT_URI=http://localhost:5173/callback
TWITCH_SCOPES=chat:read chat:edit

# Optional: League of Legends rank/game stats
# Get your API key at https://developer.riotgames.com/
RIOT_API_KEY=your_riot_api_key_here
# Platform regions: na1, euw1, eun1, kr, jp1, br1, la1, la2, oc1, tr1, ru, me1, sg2, th2, ph2, vn2, tw2
RIOT_API_REGION=na1
# Optional override for match/v5 regional routing (americas|europe|asia|sea). If unset, inferred from RIOT_API_REGION.
RIOT_API_REGIONAL=europe

# Optional settings
COMMAND_PREFIX=!
BOT_OWNER=your_twitch_username
